{
    // Place your snippets for python here. Each snippet is defined under a snippet name and has a prefix, body and 
    // description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
    // $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
    // same ids are connected.
    // Example:
    // "Print to console": {
    // 	"prefix": "log",
    // 	"body": [
    // 		"console.log('$1');",
    // 		"$2"
    // 	],
    // 	"description": "Log output to console"
    // }
    
    "Discord Cog Command": {
        "prefix": "discogcomm",
        "body": [
            "@commands.command(",
            "    brief='${1:briefDescription}',",
            "    help='${2:completeDescription}',",
            "    aliases=[${3:aliases}]",
            ")",
            "async def ${4:command}(self, ctx, ${5:arguments}):",
            "    ${6:stuff}",
        ],
        "description": "Discord Cog Command"
    },

    "Discord Command": {
        "prefix": "discordcomm",
        "body": [
            "@bot.command(",
            "    brief='${1:briefDescription}',",
            "    help='${2:completeDescription}',",
            "    aliases=[${3:aliases}]",
            ")",
            "async def ${4:command}(ctx, ${5:arguments}):",
            "    ${6:pass}",
        ],
        "description": "Discord Command"
    },

    "Python Docstring": {
        "prefix": "docstring",
        "body": [
            "\"\"\"",
            "${1:Function/Class general description}",
            "",
            "Parameters:",
            "    ${2:variableName (variableType): variableDescription.}",
            "",
            "Return value:",
            "    ${3:variableType: variableDescription.}",
            "\"\"\"",
            ""
        ],
        "description": "Python Docstring"
    },

    "Python Discord Cog": {
        "prefix": "discog",
        "body": [
            "import discord",
            "from discord.ext import commands",
            "from discord.utils import get",
            "from util import *",
            "",
            "class ${1:Cog Name}(commands.Cog):",
            "    def __init__(self, bot):",
            "        self.bot = bot",
            "",
            "def setup(bot):",
            "    bot.add_cog(${1:Cog Name}(bot))"

        ],
        "description": "Python Discord Cog"
    }
}