let g:qs_highlight_on_keys = ['f', 'F', 't', 'T']  " trigger a highlight in the appropriate direction
let g:qs_filetype_blaclist = ['startify']          " don't use plugin in startify screen
let g:qs_max_chars         = 240                   " dont highlight anything beyond 240 chars
let g:qs_lazy_highlight    = 1                     " highlight lazily
